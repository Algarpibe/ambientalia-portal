# Build del monorepo
# PAT de GitHub con scope read:packages, para instalar el paquete privado
# @algarpibe/zoho-sync (ver .npmrc). Requerido en `npm install` desde clone limpio
# y como build-arg del Dockerfile de hub-api.
NPM_TOKEN=
