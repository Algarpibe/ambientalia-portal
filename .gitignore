# Dependency directories
node_modules/

# Worktrees de Claude Code (viven dentro del repo; no versionar su contenido)
.claude/worktrees/

# Build artifacts
dist/
dist-ssr/
build/
*.tsbuildinfo
*.pyc

# Environment files
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
build_error*.log

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea/
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Data files and sample documents (kept out of version control)
data/
Archivos/
*.xlsx
*.docx
*.webp
*.csv
# Excepcion: la muestra de World Office es la fuente de verdad del formato de
# WO-sales y columns.test.ts la lee para comparar las 57 columnas. Sin versionarla,
# ese test pasa en local y falla en CI, y deja de proteger a todo el que no tenga
# el fichero en su disco. Son datos de demo del ERP, no de clientes reales.
!apps/WO-sales/DocumentosVentasEncabezadosMovimientoInventarioWO_Muestra.csv
load_lead_times.sql
Importar_*

# OS files
Thumbs.db
Desktop.ini

# Auditoría — detalle de vulnerabilidades; no versionar (repo público)
AUDIT_FINDINGS.md
AUDIT_REPORT.md
PLAN_REMEDIACION.md
